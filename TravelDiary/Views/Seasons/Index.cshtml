@{
  Layout = "_Layout";
}
@using TravelDiary.Models;

<h1>Seasons</h1>

@if (Model.Count == 0)
{
  <p>No Seasons have been added</p>
}

@foreach (Season season in Model)
{
  <p><a href="/seasons/@season.Id">@season.SeasonName</a></p>
}